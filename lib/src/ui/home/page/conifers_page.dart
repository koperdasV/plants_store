import 'package:flutter/material.dart';

class ConifersPage extends StatelessWidget {
  const ConifersPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
 return const Center(child: Text('conifers'));  }
}