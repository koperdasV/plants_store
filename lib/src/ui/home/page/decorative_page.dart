import 'package:flutter/material.dart';

class DecorativePage extends StatelessWidget {
  const DecorativePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
 return const Center(child: Text('decorative'));  }
}